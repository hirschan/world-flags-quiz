package com.example.worldflags.models

/**
 * Enum class that holds all the names of the json files. */

enum class FlagCategories {
    ANTARCTICA,
    AFRICA_UN,
    ASIA_UN,
    EUROPE_UN,
    NORTH_AMERICA_UN,
    OCEANIA_UN,
    SOUTH_AMERICA_UN,
}