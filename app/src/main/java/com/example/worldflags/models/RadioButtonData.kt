package com.example.worldflags.models

/** Data class that holds all metadata for a radio button. */

data class RadioButtonData(
    val mainText: String,
    val subText: String,
    val option: FlagCategories
)
